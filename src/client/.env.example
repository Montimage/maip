# Clerk Authentication Configuration
# 
# To enable authentication:
# 1. Sign up at https://clerk.com/
# 2. Create a new application
# 3. Copy your Publishable Key from the dashboard
# 4. Rename this file to .env
# 5. Replace the placeholder with your actual key
#
# The app will run WITHOUT authentication if this key is not set (backwards compatible)

# Clerk Publishable Key (Required for authentication)
# IMPORTANT: This project uses Create React App (CRA), which requires REACT_APP_* prefix
# DO NOT use VITE_CLERK_PUBLISHABLE_KEY - that's for Vite projects only
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Optional: Clerk Frontend API URL (usually auto-detected)
# REACT_APP_CLERK_FRONTEND_API=your-app.clerk.accounts.dev

# Optional: Custom redirect URLs
# REACT_APP_CLERK_SIGN_IN_URL=/sign-in
# REACT_APP_CLERK_SIGN_UP_URL=/sign-up
# REACT_APP_CLERK_AFTER_SIGN_IN_URL=/models/all
# REACT_APP_CLERK_AFTER_SIGN_UP_URL=/models/all

# ========================================
# Other Environment Variables
# ========================================

# Backend API URL (if needed)
# REACT_APP_API_URL=http://localhost:5000

# Analytics (if using)
# REACT_APP_ANALYTICS_KEY=your_analytics_key

# Feature flags (if needed)
# REACT_APP_ENABLE_FEATURE_X=true
